# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Source rulebook PDFs (never committed)
/sources/*.pdf
/sources/_split/

# Extraction pipeline working files — raw candidates, review artifacts, and
# curate.py's staging output. All derived; the curated result is hand-merged
# into data/<edition>/*.json, which IS committed.
/data/packs/

.env
.env.local
